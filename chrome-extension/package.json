{
  "name": "chrome-extension-svelte",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-assets",
    "copy-assets": "cp public/manifest.json dist/ && cp -r public/icons dist/ && cp src/content/*.js dist/ && cp src/background/background.js dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.0.0",
    "vite": "^5.0.0"
  }
}
